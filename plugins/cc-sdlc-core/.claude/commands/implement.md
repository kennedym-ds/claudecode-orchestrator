# /implement — TDD Execution

Implement the following using test-driven development: $ARGUMENTS

## Instructions

1. If a plan exists, follow it. If not, create a minimal plan first.
2. Write failing tests (RED)
3. Implement minimal code to pass (GREEN)
4. Refactor while keeping tests green
5. Run the full verification loop (build, test, lint, typecheck)
6. Report results

Use the implementer agent with tdd-workflow and verification-loop skills.
