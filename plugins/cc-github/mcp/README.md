# cc-github MCP

This plugin uses the official [`@modelcontextprotocol/server-github`](https://www.npmjs.com/package/@modelcontextprotocol/server-github) package — no custom server required.

The MCP server is configured in `.claude-plugin/plugin.json` via `mcpServers.github` and launched automatically by Claude Code at session start.
